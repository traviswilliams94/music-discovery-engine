import React from "react";

function TrackItem() {
    return (
        <div className="TrackList" id="Tracklist">
        
        {/* {tracks ? 
        tracks.map(track => {
          return <Track 
          key={track.name} 
          track={track}
          getID={getID}/>
        })
        : <h1>Loading...</h1>
        } */}
      </div>
    )
}






export default TrackItem;